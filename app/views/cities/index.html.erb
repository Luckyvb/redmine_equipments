<% if User.current.allowed_to?(:global_view_catalogs, nil, :global => true) %>
  <div class="contextual">
    <%= link_to(t('city.action.new.title'), {:controller => 'cities', :action => 'new'}, :class => 'icon icon-add') %>
    <%= link_to(t('catalog.global.title', count: 2), {:controller => 'catalogs', :action => 'index'}, :class => 'icon icon-list') if User.current.allowed_to?(:global_view_catalogs, nil, :global => true) %>
  </div>

  <h2><%= t('city.title', count: 2) %></h2>

  <%= render partial: 'table', locals: { cities: @cities } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
