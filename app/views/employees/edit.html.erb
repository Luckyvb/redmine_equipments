<% if User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <h1><%= t('employee.action.edit.title') %></h1>

  <%= render partial: 'form', locals: { employee: @employee, action: :update } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'employees', :action => 'index'} %>
