<% if User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <h1><%= t('service_type.action.edit.title') %></h1>

  <%= render partial: 'form', locals: { service_type: @service_type, action: :update } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'service_types', :action => 'index'} %>
