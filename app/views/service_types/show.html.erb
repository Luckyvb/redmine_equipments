<% if User.current.allowed_to?(:global_view_catalogs, nil, :global => true) %>
  <% allow_edit = User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <div class="contextual">
    <%= link_to(t('service_type.action.new.title'), {:controller => 'service_types', :action => 'new'}, :class => 'icon icon-add') if allow_edit %>
    <%= link_to(t('service_type.action.edit.title'), {:controller => 'service_types', :action => 'edit', :id => @service_type.id}, :class => 'icon icon-edit') if allow_edit && !@service_type.blank? %>
    <%= link_to(t('service_type.action.delete.title'), {:controller => 'service_types', :action => 'destroy', :id => @service_type.id}, :class => 'icon icon-del') if allow_edit && !@service_type.blank? %>
  </div>
  <% content_for :sidebar do %>
    <ul id="options">
      <li><%= link_to(t('service_type.action.new.title'), {:controller => 'service_types', :action => 'new'}, :class => 'icon icon-add') if allow_edit %></li>
      <li><%= link_to(t('service_type.action.edit.title'), {:controller => 'service_types', :action => 'edit', :id => @service_type.id}, :class => 'icon icon-edit') if allow_edit && !@service_type.blank? %></li>
      <li><%= link_to(t('service_type.action.delete.title'), {:controller => 'service_types', :action => 'destroy', :id => @service_type.id}, :class => 'icon icon-del') if allow_edit && !@service_type.blank? %></li>
    </ul>
  <% end %>
  <h2><%= t('service_type.title', count: 1) %></h2>
  <% if !@service_type.blank? %>
    <p><%= t('service_type.attr.name') %>:<%= @service_type.name %></p>
    <%= link_to t('button_cancel'), {:controller => 'service_types', :action => 'index'} %>
  <% else %>
    <p class="nodata"><%=t('data.empty.message')%></p>
  <% end %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
