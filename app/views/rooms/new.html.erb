<% if User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <h1><%= t('room.action.new.title') %> </h1>

  <%= render partial: 'form', locals: { room: @room, action: :create } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'rooms', :action => 'index'} %>
