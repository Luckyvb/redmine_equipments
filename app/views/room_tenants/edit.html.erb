<% if User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <h1><%= t('room.action.edit.title') %></h1>

  <%= render partial: 'form', locals: { room_tenant: @room_tenant, action: :update } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'room_tenants', :action => 'index'} %>
