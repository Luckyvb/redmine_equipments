<% if User.current.allowed_to?(:global_edit_catalogs, nil, :global => true) %>
  <h1><%= t('document_type.action.new.title') %> </h1>

  <%= render partial: 'form', locals: { equipment_type: @document_type, action: :create } %>
<% else %>
  <p class="nodata"><%=t('permission.denied.message')%></p>
<% end %>
<%= link_to t('button_cancel'), {:controller => 'document_types', :action => 'index'} %>
